<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="utf-8" />
    <title>Importar Alunos</title>
    <link rel="stylesheet" href="/assets/theme-d32f8eb8.css" />
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <!-- Adicione outras dependências se necessário -->
  </head>
  <body>
    <%- include('partials/sidebar') %>
    <div class="min-h-screen flex flex-col lg:ps-64 w-full">
      <%- include('partials/header') %>
      <main
        class="flex items-center justify-center min-h-screen bg-gray-50 py-12"
      >
        <section
          id="import-section"
          class="w-full max-w-2xl bg-gray-100 rounded-lg shadow-lg p-10"
        >
          <h2 class="text-2xl font-bold mb-6 text-center">Importar Alunos</h2>
          <div class="mb-6 text-center text-base text-gray-700">
            Carregue um arquivo CSV com as informações dos seus alunos do seu
            computador. Para saber mais sobre como preparar sua planilha CSV,
            <a
              target="_blank"
              rel="noopener noreferrer"
              href="/m/corp2/other/example-student-import.csv"
              class="text-blue-600 hover:underline"
              >baixe um modelo de arquivo CSV</a
            >
            ou confira nossa documentação sobre
            <a
              target="_blank"
              rel="noopener noreferrer"
              href="https://s.typingclub.com/docs/student-management/student-account-setup/add-many-students.html"
              class="text-blue-600 hover:underline"
              >como preparar os seus dados</a
            >.
          </div>
          <form
            action="/admin/aluno/importar"
            method="POST"
            enctype="multipart/form-data"
            class="space-y-6"
          >
            <div>
              <label
                for="arquivo"
                class="block text-base font-semibold text-gray-800 mb-3"
              >
                Selecione o arquivo CSV
              </label>
              <input
                type="file"
                id="arquivo"
                name="arquivo"
                accept=".csv"
                required
                class="w-full border border-gray-300 rounded-md p-3"
              />
            </div>
            <div class="flex justify-center gap-6">
              <button
                type="submit"
                class="py-3 px-6 font-semibold tracking-wide text-sm bg-blue-600 hover:bg-blue-700 border border-blue-600 hover:border-blue-700 text-white rounded-md"
              >
                Importar
              </button>
              <a
                href="/admin/aluno/cadastrar"
                class="py-3 px-6 font-semibold tracking-wide text-sm bg-red-600 hover:bg-red-700 border border-red-600 hover:border-red-700 text-white rounded-md"
              >
                Cancelar
              </a>
            </div>
          </form>
        </section>
      </main>

      <%- include('partials/footer') %>
    </div>
    <script>
      // Atualiza o texto com o nome do arquivo selecionado
      document
        .getElementById("arquivo")
        .addEventListener("change", function () {
          var fileName = this.files[0]
            ? this.files[0].name
            : "Nenhum arquivo selecionado";
          document.getElementById("nome-arquivo").textContent = fileName;
        });
    </script>
  </body>
</html>
